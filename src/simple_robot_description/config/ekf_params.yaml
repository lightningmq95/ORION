ekf_node:
  ros__parameters:
    process_noise_vx: 0.05        # Process noise for linear velocity (motion model)
    process_noise_omega: 0.4      # Process noise for angular velocity (rotation model)
    imu_heading_noise: 0.05       # Measurement noise for IMU heading (yaw)
